name: Build Windows EXE

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install MinGW
        shell: powershell
        run: |
          choco install mingw -y
          refreshenv

      - name: Compile EXE with MinGW
        shell: cmd
        run: |
          "C:\Program Files\mingw-w64\x86_64-10.3.0-posix-seh-rt_v10-rev0\mingw64\bin\g++.exe" main.cpp -mwindows -static -o rectangle.exe

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: rectangle
          path: rectangle.exe
